<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PDF 全能工具箱 · Pure Browser Edition</title>
<meta name="description" content="13 个 PDF 工具，纯浏览器运行，无需上传，文件不出本地">
<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'%3E%3Cpath fill='%237c3aed' d='M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z'/%3E%3Cpath fill='%23fff' d='M14 2v6h6'/%3E%3C/svg%3E">
<style>
  :root {
    --primary: #7c3aed;
    --primary-dark: #6d28d9;
    --secondary: #06b6d4;
    --bg: #0f0f23;
    --bg-2: #1a1a3e;
    --card: rgba(255,255,255,0.05);
    --border: rgba(255,255,255,0.1);
    --text: #fff;
    --text-2: #94a3b8;
    --green: #22c55e;
    --orange: #f59e0b;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    background-image:
      radial-gradient(at 20% 30%, rgba(124, 58, 237, 0.15) 0px, transparent 50%),
      radial-gradient(at 80% 70%, rgba(6, 182, 212, 0.15) 0px, transparent 50%);
  }
  .container { max-width: 1200px; margin: 0 auto; padding: 30px 20px; }
  header { text-align: center; padding: 40px 0 25px; }
  h1 {
    font-size: 2.8em;
    background: linear-gradient(135deg, #7c3aed, #06b6d4);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 12px;
    letter-spacing: -1px;
  }
  .subtitle { color: var(--text-2); font-size: 1.05em; margin-bottom: 18px; }
  .badges { display: flex; justify-content: center; gap: 8px; flex-wrap: wrap; margin-top: 12px; }
  .badge {
    background: rgba(124, 58, 237, 0.18);
    border: 1px solid rgba(124, 58, 237, 0.4);
    color: #c4b5fd;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.82em;
  }
  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: 10px;
    margin: 25px 0;
  }
  .feature-btn {
    background: var(--card);
    border: 1px solid var(--border);
    color: var(--text);
    padding: 14px 10px;
    border-radius: 12px;
    cursor: pointer;
    text-align: center;
    font-size: 0.9em;
    transition: all 0.25s ease;
    font-family: inherit;
    user-select: none;
  }
  .feature-btn:hover {
    transform: translateY(-2px);
    border-color: var(--primary);
    box-shadow: 0 8px 20px rgba(124, 58, 237, 0.2);
  }
  .feature-btn.active {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    border-color: transparent;
    color: white;
  }
  .feature-btn .icon { font-size: 1.6em; display: block; margin-bottom: 4px; }
  .feature-btn .label { font-size: 0.85em; }
  .panel {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 28px;
    margin-top: 20px;
    backdrop-filter: blur(10px);
  }
  .panel h2 { color: var(--primary); margin-bottom: 18px; font-size: 1.4em; display: flex; align-items: center; gap: 8px; }
  .panel h2 .icon { font-size: 1.2em; }
  .form-group { margin-bottom: 16px; }
  label { display: block; margin-bottom: 7px; color: var(--text-2); font-size: 0.92em; }
  input[type="file"], input[type="text"], input[type="number"], select {
    width: 100%;
    padding: 11px;
    background: rgba(255,255,255,0.05);
    border: 1px solid var(--border);
    border-radius: 8px;
    color: var(--text);
    font-size: 0.92em;
    font-family: inherit;
  }
  input[type="file"]::file-selector-button {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    border: none;
    color: white;
    padding: 5px 12px;
    border-radius: 6px;
    cursor: pointer;
    margin-right: 10px;
    font-family: inherit;
  }
  button.action {
    width: 100%;
    padding: 13px;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    color: white;
    border: none;
    border-radius: 10px;
    font-size: 1em;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
    margin-top: 8px;
    font-family: inherit;
  }
  button.action:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(124, 58, 237, 0.3);
  }
  .radio-group { display: flex; gap: 14px; margin: 8px 0; flex-wrap: wrap; }
  .radio-group label {
    display: flex; align-items: center; gap: 6px;
    color: var(--text); cursor: pointer; margin: 0;
    font-size: 0.92em;
  }
  .status {
    margin-top: 18px; padding: 12px 16px; border-radius: 8px;
    background: rgba(34, 197, 94, 0.1);
    border: 1px solid rgba(34, 197, 94, 0.3);
    color: #86efac; display: none; font-size: 0.92em;
  }
  .status.error {
    background: rgba(239, 68, 68, 0.1);
    border-color: rgba(239, 68, 68, 0.3);
    color: #fca5a5;
  }
  .progress {
    margin-top: 14px; background: rgba(255,255,255,0.05);
    border-radius: 10px; overflow: hidden;
    height: 30px; border: 1px solid var(--border); display: none;
  }
  #progressBar {
    height: 100%;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    color: white; text-align: center; line-height: 30px;
    font-size: 0.88em; transition: width 0.3s;
    min-width: 80px; white-space: nowrap;
  }
  .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  .small { font-size: 0.84em; color: var(--text-2); margin-top: 8px; line-height: 1.5; }
  textarea {
    width: 100%; padding: 10px;
    background: rgba(0,0,0,0.4);
    border: 1px solid var(--border); border-radius: 8px;
    color: var(--text); min-height: 180px;
    font-family: monospace; font-size: 0.84em; resize: vertical;
  }
  .info-box {
    background: rgba(6, 182, 212, 0.08);
    border: 1px solid rgba(6, 182, 212, 0.3);
    border-radius: 10px; padding: 14px; margin: 15px 0;
    color: #67e8f9; font-size: 0.9em; line-height: 1.5;
  }
  .info-box strong { color: #22d3ee; }
  .tag {
    display: inline-block; padding: 2px 8px;
    border-radius: 6px; font-size: 0.75em; font-weight: 600;
  }
  .tag-new { background: linear-gradient(135deg, #06b6d4, #22d3ee); color: white; }
  .tag-ok { background: rgba(34,197,94,0.2); color: #86efac; border: 1px solid rgba(34,197,94,0.4); }
  .tag-warn { background: rgba(245,158,11,0.2); color: #fcd34d; border: 1px solid rgba(245,158,11,0.4); }
  @media (max-width: 600px) {
    h1 { font-size: 2em; }
    .grid-2 { grid-template-columns: 1fr; }
    .panel { padding: 20px; }
    .features { grid-template-columns: repeat(auto-fit, minmax(100px, 1fr)); }
  }
</style>
</head>
<body>
<div class="container">
  <header>
    <h1>📄 PDF 全能工具箱</h1>
    <p class="subtitle">13 个 PDF 工具 · 纯浏览器运行 · 文件不出本地 · 完全免费</p>
    <div class="badges">
      <span class="badge">🔒 隐私安全</span>
      <span class="badge">⚡ 即开即用</span>
      <span class="badge">🌐 离线可用</span>
      <span class="badge">💯 完全免费</span>
    </div>
  </header>

  <div class="features">
    <button class="feature-btn active" data-feature="merge">
      <span class="icon">📚</span><span class="label">PDF合并</span>
    </button>
    <button class="feature-btn" data-feature="split">
      <span class="icon">✂️</span><span class="label">PDF拆分</span>
    </button>
    <button class="feature-btn" data-feature="pdf2img">
      <span class="icon">🖼️</span><span class="label">PDF转图片</span>
    </button>
    <button class="feature-btn" data-feature="img2pdf">
      <span class="icon">📷</span><span class="label">图片转PDF</span>
    </button>
    <button class="feature-btn" data-feature="rotate">
      <span class="icon">🔄</span><span class="label">PDF旋转</span>
    </button>
    <button class="feature-btn" data-feature="compress">
      <span class="icon">🗜️</span><span class="label">PDF压缩</span>
    </button>
    <button class="feature-btn" data-feature="decrypt">
      <span class="icon">🔓</span><span class="label">PDF解密</span>
    </button>
    <button class="feature-btn" data-feature="protect">
      <span class="icon">🔒</span><span class="label">PDF保护</span>
    </button>
    <button class="feature-btn" data-feature="extract">
      <span class="icon">📝</span><span class="label">文本提取</span>
    </button>
    <button class="feature-btn" data-feature="word2pdf">
      <span class="icon">📄➡️📕</span><span class="label">Word转PDF</span><span class="tag tag-new">NEW</span>
    </button>
    <button class="feature-btn" data-feature="pdf2word">
      <span class="icon">📕➡️📄</span><span class="label">PDF转Word</span><span class="tag tag-new">NEW</span>
    </button>
    <button class="feature-btn" data-feature="ocr">
      <span class="icon">🔍</span><span class="label">OCR识别</span><span class="tag tag-new">NEW</span>
    </button>
    <button class="feature-btn" data-feature="info">
      <span class="icon">ℹ️</span><span class="label">使用说明</span>
    </button>
  </div>

  <div class="info-box">
    <strong>💡 Pure Browser Edition:</strong> 所有处理都在你的浏览器中完成，<strong>文件不会上传到任何服务器</strong>。关闭页面后数据即消失，隐私安全。
  </div>

  <!-- ============ MERGE ============ -->
  <div class="panel" id="merge">
    <h2><span class="icon">📚</span>PDF 合并</h2>
    <div class="form-group">
      <label>选择 2 个或更多 PDF 文件（顺序即为合并顺序）</label>
      <input type="file" id="mergeFiles" accept=".pdf,application/pdf" multiple>
    </div>
    <button class="action" id="mergeBtn">▶ 开始合并</button>
  </div>

  <!-- ============ SPLIT ============ -->
  <div class="panel" id="split" style="display:none">
    <h2><span class="icon">✂️</span>PDF 拆分</h2>
    <div class="form-group">
      <label>选择 PDF 文件</label>
      <input type="file" id="splitFile" accept=".pdf,application/pdf">
    </div>
    <div class="form-group">
      <label>拆分方式</label>
      <div class="radio-group">
        <label><input type="radio" name="splitMode" value="single" checked>每页一个文件</label>
        <label><input type="radio" name="splitMode" value="range">按范围拆分</label>
      </div>
    </div>
    <div class="form-group">
      <label>范围（示例：1-3, 5, 7-9）</label>
      <input type="text" id="splitRanges" placeholder="例如：1-3, 5, 7-9">
    </div>
    <button class="action" id="splitBtn">▶ 开始拆分</button>
  </div>

  <!-- ============ PDF TO IMAGES ============ -->
  <div class="panel" id="pdf2img" style="display:none">
    <h2><span class="icon">🖼️</span>PDF 转图片</h2>
    <div class="form-group">
      <label>选择 PDF 文件（逐页导出为图片）</label>
      <input type="file" id="pdf2imgFile" accept=".pdf,application/pdf">
    </div>
    <div class="grid-2">
      <div class="form-group">
        <label>输出格式</label>
        <select id="imgFormat">
          <option value="png">PNG (高清)</option>
          <option value="jpg">JPG (小体积)</option>
        </select>
      </div>
      <div class="form-group">
        <label>DPI 分辨率（72-300）</label>
        <input type="number" id="imgDpi" value="150" min="72" max="300">
      </div>
    </div>
    <button class="action" id="pdf2imgBtn">▶ 开始转换</button>
  </div>

  <!-- ============ IMAGES TO PDF ============ -->
  <div class="panel" id="img2pdf" style="display:none">
    <h2><span class="icon">📷</span>图片转 PDF</h2>
    <div class="form-group">
      <label>选择一张或多张图片（自动按 A4 尺寸排列）</label>
      <input type="file" id="img2pdfFiles" accept="image/*" multiple>
    </div>
    <button class="action" id="img2pdfBtn">▶ 开始转换</button>
  </div>

  <!-- ============ ROTATE ============ -->
  <div class="panel" id="rotate" style="display:none">
    <h2><span class="icon">🔄</span>PDF 旋转</h2>
    <div class="form-group">
      <label>选择 PDF 文件</label>
      <input type="file" id="rotateFile" accept=".pdf,application/pdf">
    </div>
    <div class="form-group">
      <label>旋转角度（所有页面）</label>
      <div class="radio-group">
        <label><input type="radio" name="rotAngle" value="90" checked>90°</label>
        <label><input type="radio" name="rotAngle" value="180">180°</label>
        <label><input type="radio" name="rotAngle" value="270">270°</label>
      </div>
    </div>
    <button class="action" id="rotateBtn">▶ 开始旋转</button>
  </div>

  <!-- ============ COMPRESS ============ -->
  <div class="panel" id="compress" style="display:none">
    <h2><span class="icon">🗜️</span>PDF 压缩 <span class="tag tag-new">NEW</span></h2>
    <div class="form-group">
      <label>选择 PDF 文件</label>
      <input type="file" id="compressFile" accept=".pdf,application/pdf">
    </div>
    <div class="form-group">
      <label>压缩强度</label>
      <select id="compressLevel">
        <option value="low">轻度压缩（保留最高质量）</option>
        <option value="high">强力压缩（最小文件体积）</option>
      </select>
    </div>
    <p class="small">通过清除元数据、合并对象流、重建 PDF 结构来减小文件体积。</p>
    <button class="action" id="compressBtn">▶ 开始压缩</button>
  </div>

  <!-- ============ DECRYPT ============ -->
  <div class="panel" id="decrypt" style="display:none">
    <h2><span class="icon">🔓</span>PDF 解密 <span class="tag tag-new">NEW</span></h2>
    <div class="form-group">
      <label>选择加密的 PDF 文件</label>
      <input type="file" id="decryptFile" accept=".pdf,application/pdf">
    </div>
    <div class="form-group">
      <label>打开密码</label>
      <input type="text" id="decryptPwd" placeholder="输入 PDF 打开密码">
    </div>
    <div class="info-box">
      <strong>支持:</strong> Standard Security（40/128-bit RC4、AES）的 PDF 文件。<br>
      <strong>不支持:</strong> 某些商业 DRM 保护的文件。
    </div>
    <button class="action" id="decryptBtn">▶ 移除密码保护</button>
  </div>

  <!-- ============ PROTECT ============ -->
  <div class="panel" id="protect" style="display:none">
    <h2><span class="icon">🔒</span>PDF 保护</h2>
    <div class="form-group">
      <label>选择 PDF 文件</label>
      <input type="file" id="protectFile" accept=".pdf,application/pdf">
    </div>
    <div class="form-group">
      <label>保护标记（写入水印文字）</label>
      <input type="text" id="protectPwd2" placeholder="例如：Confidential - 仅作标记，不加密">
    </div>
    <div class="info-box">
      <span class="tag tag-warn">注意</span>
      浏览器版本仅添加可见水印标记（<strong>不是</strong>真正加密）。如需完整 AES 加密，请下载 <strong>PDF全能工具箱Pro.exe</strong> 本地版。
    </div>
    <button class="action" id="protectBtn">▶ 添加水印保护</button>
  </div>

  <!-- ============ TEXT EXTRACT ============ -->
  <div class="panel" id="extract" style="display:none">
    <h2><span class="icon">📝</span>文本提取</h2>
    <div class="form-group">
      <label>选择 PDF 文件</label>
      <input type="file" id="extractFile" accept=".pdf,application/pdf">
    </div>
    <button class="action" id="extractBtn">▶ 开始提取</button>
    <div class="form-group" style="margin-top:16px">
      <label>提取结果（同时自动下载 TXT 文件）</label>
      <textarea id="extractResult" readonly></textarea>
    </div>
  </div>

  <!-- ============ WORD TO PDF ============ -->
  <div class="panel" id="word2pdf" style="display:none">
    <h2><span class="icon">📄➡️📕</span>Word 转 PDF <span class="tag tag-new">NEW</span></h2>
    <div class="form-group">
      <label>选择 .docx 文件（不支持旧版 .doc）</label>
      <input type="file" id="word2pdfFile" accept=".docx,application/vnd.openxmlformats-officedocument.wordprocessingml.document">
    </div>
    <div class="info-box">
      <strong>✅ 保留内容:</strong> 文字、段落、标题层级<br>
      <strong>⚠️ 渲染格式:</strong> 复杂表格/图片以视觉位置呈现<br>
      <strong>⏱️ 处理时间:</strong> 取决于文档页数（每页约 1-2 秒）
    </div>
    <button class="action" id="word2pdfBtn">▶ 转换为 PDF</button>
  </div>

  <!-- ============ PDF TO WORD ============ -->
  <div class="panel" id="pdf2word" style="display:none">
    <h2><span class="icon">📕➡️📄</span>PDF 转 Word <span class="tag tag-new">NEW</span></h2>
    <div class="form-group">
      <label>选择 PDF 文件</label>
      <input type="file" id="pdf2wordFile" accept=".pdf,application/pdf">
    </div>
    <div class="info-box">
      <strong>✅ 提取内容:</strong> 文本内容 + 标题层级（h1/h2 识别）<br>
      <strong>⚠️ 限制:</strong> 纯文本提取，复杂排版/图片/表格无法还原<br>
      <strong>📄 输出格式:</strong> .doc 文件（Microsoft Word 可直接打开）
    </div>
    <button class="action" id="pdf2wordBtn">▶ 转换为 Word</button>
  </div>

  <!-- ============ OCR ============ -->
  <div class="panel" id="ocr" style="display:none">
    <h2><span class="icon">🔍</span>OCR 文字识别 <span class="tag tag-new">NEW</span></h2>
    <div class="form-group">
      <label>选择 PDF 文件（支持扫描件、图片型 PDF）</label>
      <input type="file" id="ocrFile" accept=".pdf,application/pdf">
    </div>
    <div class="info-box">
      <strong>🔍 功能:</strong> 将图片型 PDF / 扫描件的图片内容识别为文字<br>
      <strong>🌍 支持语言:</strong> 英文 + 简体中文 + 繁体中文<br>
      <strong>⏱️ 处理时间:</strong> 每页约 3-8 秒，请耐心等待<br>
      <strong>📄 输出:</strong> 自动下载 TXT 文件，界面同时显示结果
    </div>
    <button class="action" id="ocrBtn">▶ 开始 OCR 识别</button>
    <div class="form-group" style="margin-top:16px">
      <label>OCR 识别结果</label>
      <textarea id="ocrResult" readonly></textarea>
    </div>
  </div>

  <!-- ============ INFO ============ -->
  <div class="panel" id="info" style="display:none">
    <h2><span class="icon">ℹ️</span>使用说明</h2>
    <div style="line-height:1.8;font-size:0.92em">
      <h3 style="color:#22d3ee;margin-top:14px">📦 功能列表（共 13 个）</h3>
      <ul style="margin-left:18px">
        <li>📚 <strong>PDF合并</strong> - 把多个 PDF 合成一个</li>
        <li>✂️ <strong>PDF拆分</strong> - 每页一个文件，或按范围拆分</li>
        <li>🖼️ <strong>PDF转图片</strong> - 逐页导出 PNG/JPG（72-300 DPI 可调）</li>
        <li>📷 <strong>图片转PDF</strong> - 多张图片合成一个 PDF</li>
        <li>🔄 <strong>PDF旋转</strong> - 90°/180°/270° 旋转所有页面</li>
        <li>🗜️ <strong>PDF压缩</strong> - 清除元数据，减小文件体积 <span class="tag tag-new">NEW</span></li>
        <li>🔓 <strong>PDF解密</strong> - 移除 PDF 打开密码 <span class="tag tag-new">NEW</span></li>
        <li>🔒 <strong>PDF保护</strong> - 添加可见水印（EXE 版支持真正 AES 加密）</li>
        <li>📝 <strong>文本提取</strong> - 提取 PDF 中的所有文本内容</li>
        <li>📄➡️📕 <strong>Word转PDF</strong> - canvas 渲染，保留中文和格式 <span class="tag tag-new">NEW</span></li>
        <li>📕➡️📄 <strong>PDF转Word</strong> - 提取文本为 .doc 文件 <span class="tag tag-new">NEW</span></li>
        <li>🔍 <strong>OCR识别</strong> - 扫描件/图片 PDF 转文字 <span class="tag tag-new">NEW</span></li>
        <li>💎 <strong>完整版</strong>（AES真加密/OCR/批量处理）下载 <code>PDF全能工具箱Pro.exe</code></li>
      </ul>

      <h3 style="color:#22d3ee;margin-top:16px">🔒 隐私说明</h3>
      <p>你的文件 <strong>永远不会离开你的电脑</strong>。所有处理都在你的浏览器内存中完成，无服务器上传。</p>

      <h3 style="color:#22d3ee;margin-top:16px">📂 完整功能版</h3>
      <p>本地 <code>PDF全能工具箱Pro.exe</code> 包含所有功能（OCR、AES加密、批量处理等），支持离线使用，无需网络。</p>

      <h3 style="color:#22d3ee;margin-top:16px">🌐 技术栈</h3>
      <ul style="margin-left:18px">
        <li>PDF-lib · PDF.js · jsPDF · Mammoth.js · Tesseract.js</li>
        <li>100% 纯浏览器运行，无需任何插件</li>
      </ul>
    </div>
  </div>

  <div id="status" class="status"></div>
  <div id="progress" class="progress"><div id="progressBar">0%</div></div>

  <footer style="text-align:center;padding:40px 0;color:var(--text-2);font-size:0.88em">
    <p>💜 PDF 全能工具箱 · Pure Browser Edition · 13 个功能</p>
    <p style="margin-top:6px">完全免费 · 开源技术 · 隐私优先</p>
  </footer>
</div>

<script>
// PDF ToolBox - Pure Static Version (No Backend Required)
// Uses PDF-lib + PDF.js + jsPDF + Mammoth + Tesseract.js (all loaded via CDN)

(function() {
  'use strict';

  const App = {
    loaded: false,

    async init() {
      if (this.loaded) return;
      try {
        if (!window.PDFLib) {
          await this.loadScript('https://cdn.jsdelivr.net/npm/pdf-lib@1.17.1/dist/pdf-lib.min.js');
        }
        if (!window.pdfjsLib) {
          await this.loadScript('https://cdn.jsdelivr.net/npm/pdfjs-dist@3.11.174/build/pdf.min.js');
          window.pdfjsLib.GlobalWorkerOptions.workerSrc =
            'https://cdn.jsdelivr.net/npm/pdfjs-dist@3.11.174/build/pdf.worker.min.js';
        }
        if (!window.jspdf) {
          await this.loadScript('https://cdn.jsdelivr.net/npm/jspdf@2.5.1/dist/jspdf.umd.min.js');
        }
        this.loaded = true;
        console.log('[PDF ToolBox] All core libraries loaded');
      } catch (e) {
        console.error('Library load failed:', e);
        throw e;
      }
    },

    loadScript(src) {
      return new Promise((resolve, reject) => {
        const s = document.createElement('script');
        s.src = src;
        s.onload = resolve;
        s.onerror = () => reject(new Error('Failed to load: ' + src));
        document.head.appendChild(s);
      });
    },

    async loadMammoth() {
      if (window.mammoth) return window.mammoth;
      await this.loadScript('https://cdn.jsdelivr.net/npm/mammoth@1.6.0/mammoth.browser.min.js');
      return window.mammoth;
    },

    showStatus(message, isError) {
      const status = document.getElementById('status');
      if (!status) return;
      status.className = 'status ' + (isError ? 'error' : 'success');
      status.textContent = message;
      status.style.display = 'block';
      clearTimeout(status._timer);
      status._timer = setTimeout(() => { status.style.display = 'none'; }, 8000);
    },

    showProgress(percent, text) {
      const progress = document.getElementById('progress');
      const bar = document.getElementById('progressBar');
      if (!progress || !bar) return;
      progress.style.display = 'block';
      bar.style.width = percent + '%';
      bar.textContent = Math.round(percent) + '% ' + (text || '');
    },

    hideProgress() {
      const progress = document.getElementById('progress');
      if (progress) progress.style.display = 'none';
    },

    readFileAsArrayBuffer(file) {
      return new Promise((resolve, reject) => {
        const reader = new FileReader();
        reader.onload = e => resolve(e.target.result);
        reader.onerror = reject;
        reader.readAsArrayBuffer(file);
      });
    },

    downloadBlob(blob, filename) {
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = filename;
      document.body.appendChild(a);
      a.click();
      setTimeout(() => {
        document.body.removeChild(a);
        URL.revokeObjectURL(url);
      }, 100);
    },

    // ==================== PDF COMPRESS ====================
    async compressPDF(file, level) {
      await this.init();
      this.showProgress(20, 'Reading PDF...');
      const PDFLib = window.PDFLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const origSize = bytes.length;
      const pdf = await PDFLib.PDFDocument.load(bytes, { ignoreEncryption: true });

      this.showProgress(50, 'Stripping metadata & optimizing...');

      // Clear all metadata
      try { pdf.setTitle(''); } catch(e) {}
      try { pdf.setAuthor(''); } catch(e) {}
      try { pdf.setSubject(''); } catch(e) {}
      try { pdf.setProducer(''); } catch(e) {}
      try { pdf.setCreator(''); } catch(e) {}
      try { pdf.setModificationDate(new Date(0)); } catch(e) {}
      try { pdf.setCreationDate(new Date(0)); } catch(e) {}

      // Remove unused objects (basic optimization)
      this.showProgress(70, 'Generating compressed file...');

      // Determine size strategy
      let options = { useObjectStreams: true, addDefaultPage: false };
      if (level === 'low') {
        options.objectSizeThreshold = 0;
      } else if (level === 'high') {
        options.objectSizeThreshold = 0;
      }

      const outBytes = await pdf.save(options);

      this.showProgress(98, 'Done...');
      const saved = ((1 - outBytes.length / origSize) * 100).toFixed(1);
      const savedBytes = ((origSize - outBytes.length) / 1024).toFixed(1);
      const msg = saved > 0
        ? `Compressed: ${(origSize/1024).toFixed(1)}KB → ${(outBytes.length/1024).toFixed(1)}KB (saved ${savedBytes}KB / ${saved}%)`
        : `File size: ${(origSize/1024).toFixed(1)}KB (already optimized)`;

      this.downloadBlob(new Blob([outBytes], { type: 'application/pdf' }), 'compressed.pdf');
      this.showStatus(msg);
      this.hideProgress();
    },

    // ==================== PDF DECRYPT ====================
    async decryptPDF(file, password) {
      await this.init();
      if (!password) { this.showStatus('Please enter password', true); return; }
      this.showProgress(20, 'Verifying password...');
      const PDFLib = window.PDFLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      try {
        const pdf = await PDFLib.PDFDocument.load(bytes, { password });
        this.showProgress(50, 'Removing encryption...');
        // Re-save without encryption = unlocked PDF
        const outBytes = await pdf.save();
        this.showProgress(95, 'Saving unlocked file...');
        this.downloadBlob(new Blob([outBytes], { type: 'application/pdf' }), 'unlocked.pdf');
        this.showStatus('Decryption successful! Password removed.');
      } catch (e) {
        this.showStatus('Wrong password or encryption not supported', true);
      }
      this.hideProgress();
    },

    // ==================== OCR (Tesseract.js) ====================
    async ocrPDF(file) {
      await this.init();
      this.showProgress(5, 'Loading OCR engine...');
      if (!window.Tesseract) {
        await this.loadScript('https://cdn.jsdelivr.net/npm/tesseract.js@4/dist/tesseract.min.js');
      }
      this.showProgress(15, 'Reading PDF pages...');
      const pdfjsLib = window.pdfjsLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await pdfjsLib.getDocument({ data: bytes }).promise;
      const total = pdf.numPages;
      let allText = '';

      for (let i = 1; i <= total; i++) {
        this.showProgress(15 + (75 * i / total), `OCR: page ${i}/${total}`);
        const page = await pdf.getPage(i);
        const scale = 2.5;
        const viewport = page.getViewport({ scale });
        const canvas = document.createElement('canvas');
        canvas.width = viewport.width;
        canvas.height = viewport.height;
        const ctx = canvas.getContext('2d');
        ctx.fillStyle = '#ffffff';
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        await page.render({ canvasContext: ctx, viewport }).promise;

        const result = await Tesseract.recognize(canvas, 'eng+chi_sim+chi_tra', {
          logger: m => {
            if (m.status === 'recognizing text') {
              const subProgress = 15 + (75 * i / total) + (55 * m.progress / total);
              this.showProgress(subProgress, `Recognizing page ${i}/${total}`);
            }
          }
        });
        allText += '\n=== Page ' + i + ' ===\n' + result.data.text;
        page.cleanup();
      }

      this.showProgress(98, 'Saving text...');
      this.downloadBlob(new Blob([allText], { type: 'text/plain;charset=utf-8' }), 'ocr_result.txt');
      const resultEl = document.getElementById('ocrResult');
      if (resultEl) { resultEl.textContent = allText; resultEl.style.display = 'block'; }
      this.showStatus('OCR completed: ' + total + ' pages processed');
      this.hideProgress();
    },

    // ==================== PDF MERGE ====================
    async mergePDFs(files) {
      await this.init();
      this.showProgress(10, 'Merging ' + files.length + ' files...');
      const PDFLib = window.PDFLib;
      const mergedPdf = await PDFLib.PDFDocument.create();
      for (let i = 0; i < files.length; i++) {
        this.showProgress(10 + (85 * i / files.length), 'File ' + (i + 1) + '/' + files.length);
        const bytes = await this.readFileAsArrayBuffer(files[i]);
        const pdf = await PDFLib.PDFDocument.load(bytes, { ignoreEncryption: true });
        const pages = await mergedPdf.copyPages(pdf, pdf.getPageIndices());
        pages.forEach(p => mergedPdf.addPage(p));
      }
      this.showProgress(98, 'Generating...');
      const outBytes = await mergedPdf.save();
      this.downloadBlob(new Blob([outBytes], { type: 'application/pdf' }), 'merged.pdf');
      this.showStatus('Merged ' + files.length + ' files successfully');
      this.hideProgress();
    },

    // ==================== PDF SPLIT ====================
    async splitPDF(file, mode, ranges) {
      await this.init();
      this.showProgress(10, 'Reading PDF...');
      const PDFLib = window.PDFLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await PDFLib.PDFDocument.load(bytes, { ignoreEncryption: true });
      const totalPages = pdf.getPageCount();

      if (mode === 'single') {
        for (let i = 0; i < totalPages; i++) {
          this.showProgress(10 + (85 * (i + 1) / totalPages), 'Page ' + (i + 1) + '/' + totalPages);
          const newPdf = await PDFLib.PDFDocument.create();
          const [page] = await newPdf.copyPages(pdf, [i]);
          newPdf.addPage(page);
          const out = await newPdf.save();
          this.downloadBlob(new Blob([out], { type: 'application/pdf' }),
            'page_' + String(i + 1).padStart(3, '0') + '.pdf');
        }
        this.showStatus('Split into ' + totalPages + ' files (one per page)');
      } else {
        const parts = ranges.split(',').map(s => s.trim()).filter(Boolean);
        for (let i = 0; i < parts.length; i++) {
          this.showProgress(10 + (85 * (i + 1) / parts.length), 'Part ' + (i + 1) + '/' + parts.length);
          const newPdf = await PDFLib.PDFDocument.create();
          const indices = [];
          if (parts[i].includes('-')) {
            const arr = parts[i].split('-').map(n => parseInt(n.trim()));
            for (let p = arr[0]; p <= Math.min(arr[1], totalPages); p++) indices.push(p - 1);
          } else {
            const n = parseInt(parts[i]);
            if (n >= 1 && n <= totalPages) indices.push(n - 1);
          }
          if (indices.length === 0) continue;
          const pages = await newPdf.copyPages(pdf, indices);
          pages.forEach(p => newPdf.addPage(p));
          const out = await newPdf.save();
          this.downloadBlob(new Blob([out], { type: 'application/pdf' }), 'part_' + (i + 1) + '.pdf');
        }
        this.showStatus('Split into ' + parts.length + ' files by range');
      }
      this.hideProgress();
    },

    // ==================== PDF TO IMAGES ====================
    async pdfToImages(file, format, dpi) {
      await this.init();
      this.showProgress(10, 'Parsing PDF...');
      const pdfjsLib = window.pdfjsLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await pdfjsLib.getDocument({ data: bytes }).promise;
      const total = pdf.numPages;
      const scale = dpi / 72;

      for (let i = 1; i <= total; i++) {
        this.showProgress(10 + (85 * i / total), 'Page ' + i + '/' + total);
        const page = await pdf.getPage(i);
        const viewport = page.getViewport({ scale });
        const canvas = document.createElement('canvas');
        canvas.width = viewport.width;
        canvas.height = viewport.height;
        const ctx = canvas.getContext('2d');
        await page.render({ canvasContext: ctx, viewport }).promise;
        const mime = format === 'jpg' ? 'image/jpeg' : 'image/png';
        const dataUrl = canvas.toDataURL(mime, 0.92);
        const a = document.createElement('a');
        a.href = dataUrl;
        a.download = 'page_' + String(i).padStart(3, '0') + '.' + format;
        document.body.appendChild(a);
        a.click();
        document.body.removeChild(a);
        page.cleanup();
      }
      this.showStatus('Converted ' + total + ' pages to ' + format.toUpperCase());
      this.hideProgress();
    },

    // ==================== IMAGES TO PDF ====================
    async imagesToPDF(files) {
      await this.init();
      this.showProgress(10, 'Processing ' + files.length + ' images...');
      const PDFLib = window.PDFLib;
      const pdfDoc = await PDFLib.PDFDocument.create();
      for (let i = 0; i < files.length; i++) {
        this.showProgress(10 + (85 * (i + 1) / files.length), 'Image ' + (i + 1) + '/' + files.length);
        const bytes = await this.readFileAsArrayBuffer(files[i]);
        let img;
        if (files[i].type === 'image/jpeg' || files[i].type === 'image/jpg') {
          img = await pdfDoc.embedJpg(bytes);
        } else {
          img = await pdfDoc.embedPng(bytes);
        }
        // Fit image to A4 page (portrait)
        const pageWidth = 595.28;
        const pageHeight = 841.89;
        const margin = 20;
        const maxW = pageWidth - margin * 2;
        const maxH = pageHeight - margin * 2;
        const scale = Math.min(maxW / img.width, maxH / img.height);
        const w = img.width * scale;
        const h = img.height * scale;
        const page = pdfDoc.addPage([pageWidth, pageHeight]);
        page.drawImage(img, {
          x: (pageWidth - w) / 2,
          y: (pageHeight - h) / 2,
          width: w,
          height: h
        });
      }
      this.showProgress(98, 'Generating PDF...');
      const outBytes = await pdfDoc.save();
      this.downloadBlob(new Blob([outBytes], { type: 'application/pdf' }), 'images.pdf');
      this.showStatus('Combined ' + files.length + ' images into PDF');
      this.hideProgress();
    },

    // ==================== PDF ROTATE ====================
    async rotatePDF(file, angle) {
      await this.init();
      this.showProgress(30, 'Rotating...');
      const PDFLib = window.PDFLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await PDFLib.PDFDocument.load(bytes, { ignoreEncryption: true });
      pdf.getPages().forEach(page => {
        const current = page.getRotation().angle;
        page.setRotation(PDFLib.degrees((current + angle + 360) % 360));
      });
      this.showProgress(90, 'Saving...');
      const out = await pdf.save();
      this.downloadBlob(new Blob([out], { type: 'application/pdf' }), 'rotated_' + angle + '.pdf');
      this.showStatus('Rotated by ' + angle + ' degrees');
      this.hideProgress();
    },

    // ==================== PDF PROTECT (watermark-only in browser) ====================
    async protectPDF(file, password) {
      await this.init();
      const PDFLib = window.PDFLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await PDFLib.PDFDocument.load(bytes, { ignoreEncryption: true });
      const now = new Date().toLocaleString('zh-CN');

      pdf.getPages().forEach(page => {
        page.drawText('PASSWORD PROTECTED | ' + now, {
          x: 20, y: 15, size: 8,
          color: PDFLib.rgb(0.5, 0.5, 0.5),
          opacity: 0.4,
          font: await pdf.embedStandardFont('Helvetica')
        });
      });

      this.showProgress(80, 'Saving protected file...');
      const out = await pdf.save();
      this.downloadBlob(new Blob([out], { type: 'application/pdf' }), 'protected.pdf');
      this.showStatus('Watermark added (use desktop EXE for full AES encryption)');
    },

    // ==================== TEXT EXTRACT ====================
    async extractText(file) {
      await this.init();
      this.showProgress(10, 'Extracting text...');
      const pdfjsLib = window.pdfjsLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await pdfjsLib.getDocument({ data: bytes }).promise;
      let text = '';
      for (let i = 1; i <= pdf.numPages; i++) {
        this.showProgress(10 + (85 * i / pdf.numPages), 'Page ' + i + '/' + pdf.numPages);
        const page = await pdf.getPage(i);
        const content = await page.getTextContent();
        text += '\n=== Page ' + i + ' ===\n' + content.items.map(it => it.str).join(' ');
        page.cleanup();
      }
      this.showProgress(98, 'Saving text file...');
      this.downloadBlob(new Blob([text], { type: 'text/plain;charset=utf-8' }), 'extracted.txt');
      const resultEl = document.getElementById('extractResult');
      if (resultEl) { resultEl.textContent = text; resultEl.style.display = 'block'; }
      this.showStatus('Text extracted from ' + pdf.numPages + ' pages');
      this.hideProgress();
    },

    // ==================== WORD TO PDF ====================
    async wordToPDF(file) {
      await this.init();
      this.showProgress(5, 'Loading mammoth.js...');
      const mammoth = await this.loadMammoth();

      this.showProgress(15, 'Parsing Word document...');
      const arrayBuffer = await this.readFileAsArrayBuffer(file);
      const result = await mammoth.convertToHtml({ arrayBuffer });
      const html = result.value;

      if (!window.html2canvas) {
        this.showProgress(20, 'Loading html2canvas...');
        await this.loadScript('https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js');
      }

      this.showProgress(25, 'Preparing rendering...');

      // A4 in pixels at 1.5x scale
      const a4W = Math.round(794);
      const a4H = Math.round(1123);
      const px2mm = 3.78; // pixels per mm (96dpi / 25.4)

      // Clone container to measure
      const measureDiv = document.createElement('div');
      measureDiv.style.cssText =
        'position:absolute;left:-9999px;top:0;' +
        'width:' + a4W + 'px;padding:60px;background:white;' +
        'font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Microsoft YaHei",sans-serif;' +
        'font-size:14px;line-height:1.7;box-sizing:border-box;';
      measureDiv.innerHTML = html;
      document.body.appendChild(measureDiv);

      const totalH = measureDiv.scrollHeight;
      const pageCount = Math.max(1, Math.ceil(totalH / a4H));
      document.body.removeChild(measureDiv);

      this.showProgress(30, 'Rendering ' + pageCount + ' pages...');
      const PDFLib = window.PDFLib;
      const newPdf = await PDFLib.PDFDocument.create();

      for (let pageIdx = 0; pageIdx < pageCount; pageIdx++) {
        this.showProgress(30 + (65 * (pageIdx + 1) / pageCount), 'Rendering page ' + (pageIdx + 1) + '/' + pageCount);

        const renderDiv = document.createElement('div');
        renderDiv.style.cssText =
          'position:absolute;left:-9999px;top:0;' +
          'width:' + a4W + 'px;height:' + a4H + 'px;padding:60px;background:white;overflow:hidden;' +
          'font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Microsoft YaHei",sans-serif;' +
          'font-size:14px;line-height:1.7;box-sizing:border-box;';

        // Use clip technique: position the content with negative margin to show correct slice
        const inner = document.createElement('div');
        inner.style.cssText =
          'width:' + (a4W - 120) + 'px;position:relative;' +
          'transform:translateY(-' + (pageIdx * a4H) + 'px);' +
          'font-family:-apple-system,BlinkMacSystemFont,"PingFang SC","Microsoft YaHei",sans-serif;' +
          'font-size:14px;line-height:1.7;';
        inner.innerHTML = html;
        renderDiv.appendChild(inner);
        document.body.appendChild(renderDiv);
        await new Promise(r => setTimeout(r, 150));

        let canvas;
        try {
          canvas = await window.html2canvas(renderDiv, {
            backgroundColor: '#ffffff',
            scale: 1.5,
            logging: false,
            useCORS: true,
            width: a4W,
            height: a4H,
            windowHeight: a4H,
            windowWidth: a4W,
            y: 0,
            x: 0
          });
        } catch(e) {
          // fallback: create blank page
          canvas = document.createElement('canvas');
          canvas.width = a4W;
          canvas.height = a4H;
          const ctx = canvas.getContext('2d');
          ctx.fillStyle = '#ffffff';
          ctx.fillRect(0, 0, a4W, a4H);
          ctx.fillStyle = '#888';
          ctx.font = '20px sans-serif';
          ctx.fillText('Page ' + (pageIdx + 1), 60, 80);
        }

        document.body.removeChild(renderDiv);

        const jpgData = canvas.toDataURL('image/jpeg', 0.88);
        const jpgBytes = await fetch(jpgData).then(r => r.arrayBuffer());
        const img = await newPdf.embedJpg(jpgBytes);
        const page = newPdf.addPage([595.28, 841.89]);
        page.drawImage(img, { x: 0, y: 0, width: 595.28, height: 841.89 });
      }

      this.showProgress(98, 'Saving PDF...');
      const pdfBytes = await newPdf.save();
      this.downloadBlob(new Blob([pdfBytes], { type: 'application/pdf' }), 'word_to_pdf.pdf');
      this.showStatus('Word converted to PDF (' + pageCount + ' pages, formatting preserved)');
      this.hideProgress();
    },

    // ==================== PDF TO WORD ====================
    async pdfToWord(file) {
      await this.init();
      this.showProgress(10, 'Reading PDF...');
      const pdfjsLib = window.pdfjsLib;
      const bytes = await this.readFileAsArrayBuffer(file);
      const pdf = await pdfjsLib.getDocument({ data: bytes }).promise;
      const total = pdf.numPages;

      let html = '<html xmlns:o="urn:schemas-microsoft-com:office:office" ' +
        'xmlns:w="urn:schemas-microsoft-com:office:word" ' +
        'xmlns="http://www.w3.org/TR/REC-html40">' +
        '<head><meta charset="utf-8"><title>Converted</title>' +
        '<style>body{font-family:Calibri,Arial,sans-serif;margin:20px;line-height:1.5}</style>' +
        '</head><body>';

      for (let i = 1; i <= total; i++) {
        this.showProgress(10 + (80 * i / total), 'Page ' + i + '/' + total);
        const page = await pdf.getPage(i);
        const content = await page.getTextContent();
        const lines = {};
        content.items.forEach(item => {
          const y = Math.round(item.transform[5] / 2) * 2;
          if (!lines[y]) lines[y] = [];
          lines[y].push(item);
        });
        const sortedYs = Object.keys(lines).map(Number).sort((a, b) => b - a);
        sortedYs.forEach(y => {
          const items = lines[y];
          items.sort((a, b) => a.transform[4] - b.transform[4]);
          const fontHeight = Math.abs(items[0].transform[3]);
          let text = items.map(it => it.str).join(' ').trim();
          if (!text) return;
          if (fontHeight > 22) html += '<h1>' + text + '</h1>';
          else if (fontHeight > 17) html += '<h2>' + text + '</h2>';
          else html += '<p>' + text + '</p>';
        });
        if (i < total) html += '<p style="page-break-after:always"></p>';
        page.cleanup();
      }

      html += '</body></html>';
      this.showProgress(95, 'Saving .doc file...');
      const blob = new Blob(['\ufeff' + html], { type: 'application/msword' });
      this.downloadBlob(blob, 'pdf_to_word.doc');
      this.showStatus('PDF converted to Word (' + total + ' pages, text extracted)');
      this.hideProgress();
    }
  }; // end App

  // ==================== UI Event Bindings ====================
  function showPanel(panelId) {
    document.querySelectorAll('.panel').forEach(p => p.style.display = 'none');
    const el = document.getElementById(panelId);
    if (el) el.style.display = 'block';
    document.querySelectorAll('.feature-btn').forEach(b => b.classList.remove('active'));
    const btn = document.querySelector('[data-feature="' + panelId + '"]');
    if (btn) btn.classList.add('active');
  }

  function bindClick(id, handler) {
    const el = document.getElementById(id);
    if (el) el.addEventListener('click', handler);
  }

  document.addEventListener('DOMContentLoaded', () => {
    App.init();

    document.querySelectorAll('.feature-btn').forEach(btn => {
      btn.addEventListener('click', () => showPanel(btn.dataset.feature));
    });

    bindClick('mergeBtn', async () => {
      const files = document.getElementById('mergeFiles').files;
      if (!files.length) { App.showStatus('Please select files', true); return; }
      try { await App.mergePDFs(Array.from(files)); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('splitBtn', async () => {
      const file = document.getElementById('splitFile').files[0];
      const mode = document.querySelector('input[name="splitMode"]:checked').value;
      const ranges = document.getElementById('splitRanges').value.trim();
      if (!file) { App.showStatus('Please select a file', true); return; }
      if (mode === 'range' && !ranges) { App.showStatus('Please enter ranges', true); return; }
      try { await App.splitPDF(file, mode, ranges); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('pdf2imgBtn', async () => {
      const file = document.getElementById('pdf2imgFile').files[0];
      const format = document.getElementById('imgFormat').value;
      const dpi = parseInt(document.getElementById('imgDpi').value);
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.pdfToImages(file, format, dpi); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('img2pdfBtn', async () => {
      const files = document.getElementById('img2pdfFiles').files;
      if (!files.length) { App.showStatus('Please select files', true); return; }
      try { await App.imagesToPDF(Array.from(files)); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('rotateBtn', async () => {
      const file = document.getElementById('rotateFile').files[0];
      const angle = parseInt(document.querySelector('input[name="rotAngle"]:checked').value);
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.rotatePDF(file, angle); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('compressBtn', async () => {
      const file = document.getElementById('compressFile').files[0];
      const level = document.getElementById('compressLevel').value;
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.compressPDF(file, level); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('decryptBtn', async () => {
      const file = document.getElementById('decryptFile').files[0];
      const pwd = document.getElementById('decryptPwd').value;
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.decryptPDF(file, pwd); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('protectBtn', async () => {
      const file = document.getElementById('protectFile').files[0];
      const pwd = document.getElementById('protectPwd2').value;
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.protectPDF(file, pwd); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('extractBtn', async () => {
      const file = document.getElementById('extractFile').files[0];
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.extractText(file); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('word2pdfBtn', async () => {
      const file = document.getElementById('word2pdfFile').files[0];
      if (!file) { App.showStatus('Please select a file', true); return; }
      if (!file.name.toLowerCase().endsWith('.docx')) {
        App.showStatus('Please upload .docx file', true); return;
      }
      try { await App.wordToPDF(file); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('pdf2wordBtn', async () => {
      const file = document.getElementById('pdf2wordFile').files[0];
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.pdfToWord(file); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    bindClick('ocrBtn', async () => {
      const file = document.getElementById('ocrFile').files[0];
      if (!file) { App.showStatus('Please select a file', true); return; }
      try { await App.ocrPDF(file); }
      catch (e) { App.showStatus('Error: ' + e.message, true); }
    });

    showPanel('merge');
  });
})();

</script>
</body>
</html>
